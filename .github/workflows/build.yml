name: Build

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  build-test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          # .NET 10 SDK required for Microsoft.Testing.Platform (global.json); app still targets net8.0
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore SqlMcpServer.sln

      - name: Build
        run: dotnet build SqlMcpServer.sln --configuration Release --no-restore

      # Integration tests need a live SQL Server; skip them in CI.
      - name: Test (exclude integration)
        run: >
          dotnet test --solution SqlMcpServer.sln
          --configuration Release
          --no-build
          --verbosity normal
          --filter "TestCategory!=Integration"
